package com.medco.HealthConnectProvider.utils.enums;

public enum LimitType {
    AMOUNT,     // Monetary limit (e.g., $1000)
    QUANTITY,   // Quantity limit (e.g., 10 items)
    VISITS      // Visit count limit (e.g., 5 visits)
}
