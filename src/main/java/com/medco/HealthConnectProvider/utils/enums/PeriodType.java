package com.medco.HealthConnectProvider.utils.enums;

public enum PeriodType {
    ANNUAL,     // Yearly reset
    MONTHLY,    // Monthly reset
    PER_CLAIM,  // Per claim basis
    CONTRACT_PERIOD // Reset based on contract period
}
