<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.3.xsd">

    <changeSet id="fix-attachment-data-column-type" author="system">
        <comment>Fix attachment_data column type from OID to BYTEA to resolve JPA mapping issues</comment>

        <!-- Drop column if it exists (simple approach) -->
        <sql>
            ALTER TABLE medication_dispensing DROP COLUMN IF EXISTS attachment_data;
        </sql>

        <!-- Recreate column with correct BYTEA type -->
        <sql>
            ALTER TABLE medication_dispensing ADD COLUMN attachment_data BYTEA;
        </sql>

        <!-- Ensure other attachment columns exist with correct types -->
        <sql>
            ALTER TABLE medication_dispensing ADD COLUMN IF NOT EXISTS attachment_file_name VARCHAR(255);
        </sql>

        <sql>
            ALTER TABLE medication_dispensing ADD COLUMN IF NOT EXISTS attachment_content_type VARCHAR(100);
        </sql>

    </changeSet>

</databaseChangeLog>
